using Xunit;

namespace UnitTests.ChartServiceProject;

public class ProjectTests
{
    [Fact]
    public void GetProjectNameTest()
    {
        // const string projectName = "abracadabra";
        // var fileName1 = Project.GetFileName(projectName);
        // var fileName2 =  Project.GetFileName(fileName1);
        //
        // Assert.Equal(projectName, Project.GetProjectName(fileName1));
        // Assert.Equal(fileName1, Project.GetProjectName(fileName2));
        // Assert.Throws<ArgumentNullException>(() => Project.GetProjectName(null));
        // Assert.Throws<ArgumentNullException>(() => Project.GetProjectName(string.Empty));
        // Assert.Throws<ArgumentNullException>(() => Project.GetProjectName("   "));
    }
}